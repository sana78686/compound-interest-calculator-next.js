# Copy to .env.local for development

NEXT_PUBLIC_CMS_API_URL=https://app.apimstec.com
NEXT_PUBLIC_SITE_DOMAIN=compound-interest.example
# Must include http:// or https:// (e.g. http://localhost:3004). A bare host breaks metadata and causes 500 errors.
NEXT_PUBLIC_SITE_ORIGIN=https://www.compound-interest.example
